version: 2

sources:
  - name: madrid_meteo_raw
    database: CALIDAD_AIRE_PIPELINE_FIORELHA
    schema: RAW
    config:
      loaded_at_field: loaded_at
      freshness:
        warn_after:
          count: 24
          period: hour
        error_after:
          count: 48
          period: hour
    tables:
      - name: STG_METEO_HORIZONTAL